import React from 'react';import {useSelector}from 'react-redux';const SummaryPanel=()=>{const items=useSelector(s=>s.transactions.items);const income=items.filter(i=>i.type==='income').reduce((a,i)=>a+Number(i.amount||0),0);const expense=items.filter(i=>i.type==='expense').reduce((a,i)=>a+Number(i.amount||0),0);return(<section><div>Income:{income}</div><div>Expenses:{expense}</div><div>Net:{income-expense}</div></section>);};export default SummaryPanel;